<!DOCTYPE html>
<html>
  <head>
    <title>Ads - Nuvem</title>
    <style>
      form {
        max-width: 600px;
        padding: 2rem;
        border: 1px solid #ccc;
        border-radius: 10px;
      }
      
      label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: bold;
      }
      
      input[type="text"], input[type="number"], select {
        display: block;
        width: 100%;
        padding: 0.5rem;
        margin-bottom: 1rem;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 1rem;
      }
      
      button[type="submit"] {
        display: block;
        margin-top: 1rem;
        padding: 0.5rem;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem;
        transition: background-color 0.2s ease-in-out;
      }
      
      button[type="submit"]:hover {
        background-color: #0062cc;
      }
    </style>
  </head>
  <body>
    <h1>Atualizar ID: <%= carro.objectId %></h1>
    <form method="post" action="/api/update/<%= carro.objectId %>" enctype="application/x-www-form-urlencoded">
      <label for="name">Nome:</label>
      <input type="text" id="name" name="name" value="<%= carro.name %>" required >

      <label for="price">Preco:</label>
      <input type="number" id="price" name="price" min="0" step="0.01" value="<%= carro.price %>" required>

      <label for="color">Cor:</label>
      <input type="text" id="color" name="color" value="<%= carro.color %>" required>

      <button type="submit">Atualizar</button>
    </form>
  </body>
</html>